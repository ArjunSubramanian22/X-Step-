# Exploratory notebooks

Keep notebooks for illustration only. Do not commit large outputs, PHI, or untracked model dumps.

Preferred path for paper numbers: `research/experiments/run_research.py` → CSV/JSON → `inject_results.py`.
